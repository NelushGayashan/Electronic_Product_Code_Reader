#include "Keypad.h"
#include <iostream>

void Keypad::initialize()
{
    // Initialize the Keypad (replace with actual Keypad initialization code)
    std::cout << "Keypad Initialized" << std::endl;
}

char Keypad::getKey()
{
    // Simulate getting a keypress (replace with actual Keypad reading code)
    std::cout << "Keypad Key Pressed" << std::endl;
    return '1'; // Example key
}
